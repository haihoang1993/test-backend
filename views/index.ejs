<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
     </head>
  <body>
    <h1><%= title %></h1>
    <h2>Test Realtime twitter</h2>
    <p id='conten-tw'> {show data} </p>
    <hr>
    <script src="/js/socket.js"></script>
    <script>
        const socket = io.connect('http://test.allwilder.net/');
        
        socket.on('twitter', function(data){
          console.log(data);
          const { text }=data;
          document.getElementById("conten-tw").innerText=text;
        });
        socket.on('discord ', function(data){
        
        });
        socket.on('token', function(data){
          console.log('token:',data);
        });

        socket.on('page-price', function(data){
          console.log('page-price:',data);
        });
    </script>

  </body>
</html>
